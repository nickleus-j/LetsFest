@model IEnumerable<LetsFest.Data.Dto.EventCreateEditDto>

@{
    ViewData["Title"] = "Events";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-info">@Resource.AddNew @Resource.Event</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InitiatorId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.inUse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.isPublic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProposedStartDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProposedEndDateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedOn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td class="profile" data-profileId="@item.InitiatorId">
                @* @Html.DisplayFor(modelItem => item.InitiatorId) *@
               
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.inUse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isPublic)
            </td>
            <td>
                    @item.ProposedStartDateTime?.ToString("g")
            </td>
            <td>
                    @item.ProposedEndDateTime?.ToString("g")
            </td>
            <td>
                    @item.CreatedOn?.ToLocalTime().ToString("g")
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
@section Scripts {
    <script>
            // Function to load the partial view
        async function loadPartialView(userId,index) {
        // Construct the URL to your controller action, including any query parameters
        const url = `/UserProfile/ProfileLink?userId=${userId}`;

        try {
            const response = await fetch(url);

            if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
            }

            // Get the response as text (which will be the HTML of the partial view)
            const partialViewHtml = await response.text();

            // Find the container element where you want to insert the partial view
                let container = document.querySelectorAll(`.profile`)[index];

            if (container) {
                // Insert the HTML into the container
                container.innerHTML = partialViewHtml;
            } else {
                console.error('Container element not found!');
            }

        } catch (error) {
            console.error('There was a problem with the fetch operation:', error);
        }
    }
    window.onload = (event) => { 
                document.querySelectorAll("td.profile").forEach(function(item, index) {
                    loadPartialView(item.getAttribute("data-profileId"),index);
                });
    };
    </script>
}